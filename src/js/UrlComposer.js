var config = require('./../../config');

_filters = {};

module.exports = {
    addFilter: function (filterConfig) {

    },

    clearFilters: function () {

    },

    compile: function () {
        return config.baseUrl;
    }
};